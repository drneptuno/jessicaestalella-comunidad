// Esquema Drizzle — fuente de verdad de la base de datos.
//
// F1 agregará las tablas de Better Auth (user, session, account, verification)
// y la tabla `invitation` (código hasheado, email destino, estado, expiración).
// F2 agregará `profile`. Por ahora, vacío para dejar el pipeline de migraciones
// listo (drizzle-kit generate / migrate / studio).

export {}
